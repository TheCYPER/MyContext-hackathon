import { ATLAS_LANES, layoutAtlas } from "../../lib/model.mjs";
import type { GraphEdge, GraphNode } from "../../types";

export function GlobalGraph({ nodes, relations, onFocus }: { nodes: GraphNode[]; relations: GraphEdge[]; onFocus: (id: string) => void }) {
  const layout = layoutAtlas(nodes);
  const positions = layout.positions as Map<string, { x: number; y: number; width: number; height: number }>;
  return <details className="mt-5 rounded-xl border bg-card"><summary className="cursor-pointer px-5 py-4 text-sm font-medium">Global overview <span className="ml-2 text-xs font-normal text-muted-foreground">Secondary · all visible legacy links</span></summary><div className="overflow-auto border-t"><svg viewBox={`0 0 ${layout.width} ${layout.height}`} className="block min-w-[1100px]" aria-label="Global context graph">{ATLAS_LANES.map((lane: { type: string; label: string; x: number; width: number }) => <g key={lane.type}><text x={lane.x} y={30} className="graph-lane-title">{lane.label}</text><line x1={lane.x} y1={42} x2={lane.x + lane.width} y2={42} className="graph-lane-line" /></g>)}{relations.map((edge) => { const from = positions.get(edge.from); const to = positions.get(edge.to); if (!from || !to) return null; return <line key={edge.id} x1={from.x + from.width / 2} y1={from.y + from.height / 2} x2={to.x + to.width / 2} y2={to.y + to.height / 2} className="graph-edge graph-edge-global" />; })}{layout.nodes.map((node: GraphNode) => { const box = positions.get(node.id)!; return <g key={node.id} role="button" tabIndex={0} aria-label={`Focus on ${node.title}`} className="graph-node" onClick={() => onFocus(node.id)} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") onFocus(node.id); }}><rect x={box.x} y={box.y} width={box.width} height={box.height} rx={10} /><text x={box.x + 12} y={box.y + 23}><tspan className="graph-node-title">{node.title.length > 26 ? `${node.title.slice(0, 24)}…` : node.title}</tspan><tspan x={box.x + 12} dy={17} className="graph-node-meta">{node.type}</tspan></text></g>; })}</svg></div></details>;
}
