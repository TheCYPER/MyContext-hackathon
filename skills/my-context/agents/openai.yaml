interface:
  display_name: "My Context"
  short_description: "Retrieve and capture academic and work context"
  default_prompt: "Use $my-context to retrieve relevant context and capture durable academic or work outcomes under my library's write policy."
policy:
  allow_implicit_invocation: true
